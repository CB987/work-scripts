#!/bin/env python

import os

def main():
    # want to send an email with the file size of the curl log after it's done running
    size = os.path.getsize(/ezpaarse/work/curl.log)
    print size
    # if ()
    # send_email(cbarto8@emory.edu, "curl log is " + size + "bytes")
    # and maybe stop the process if it's not the right size?

if __name_ =="__main__":
    sys.exit(main())
